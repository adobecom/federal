federal/libs/global-navigation/dist/** linguist-generated=true

# The committed gnav bundle is a generated artifact, owned entirely by CI:
# feature branches never modify it (the pre-commit hook keeps it unstaged and
# the check-dist job blocks any PR that touches it), and the rebuild-dist
# workflow regenerates it on push to stage/main. Because only CI writes it,
# linearly, it never participates in a feature-branch merge — so no merge
# driver is needed here. We only suppress its (huge, minified) textual diff.
federal/libs/global-navigation/dist/main.js     -diff
federal/libs/global-navigation/dist/main.js.map -diff
