federal/libs/global-navigation/dist/** linguist-generated=true

# The committed bundle is a huge minified blob and is owned entirely by CI
# (the `rebuild-dist` workflow rebuilds it on push to main/stage; feature
# branches never touch it — see scripts/hooks/pre-commit and the check-dist
# job in merge_to_stage.yml). We only suppress textual diffs for it here;
# there is no merge driver, because nothing should ever merge it.
#
#   -diff  → don't render textual diffs for these unreadable minified blobs.
federal/libs/global-navigation/dist/main.js     -diff
federal/libs/global-navigation/dist/main.js.map -diff
