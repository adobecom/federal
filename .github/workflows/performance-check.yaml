name: Performance Check
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  perf:
    uses: adobecom/perf-metrics/.github/workflows/performance-check.yaml@main
    with:
      baseline_path: ".github/performance/baseline.json"
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
