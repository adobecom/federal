name: Release

on:
  push:
    branches:
      - main
      - stage

jobs:
  clear-stage-cache:
    if: github.ref == 'refs/heads/stage'
    uses: ./.github/workflows/clear-cache.yml
    with:
      cpCode: 1684057
      network: production
      command: delete
    secrets: inherit

  clear-prod-cache:
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/clear-cache.yml
    with:
      cpCode: 1585265
      network: production
      command: delete

    secrets: inherit
